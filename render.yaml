services:
  - type: web
    name: eduflow
    runtime: docker
    envVars:
      - key: NODE_ENV
        value: production
      - key: PROJECTS_DIR
        value: /data/projects
    disk:
      name: eduflow-data
      mountPath: /data/projects
      sizeGB: 1
